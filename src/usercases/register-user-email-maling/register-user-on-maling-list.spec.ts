import { UserData } from "./UserData"


describe('Register user on maling list user case', () => {
  test('should add user with complete data to maling list', async () => {
    const users: UserData[] = []
    console.log(users)
    // const repo: UserRepository = new inMermoryRepository(users)
    // const useCase: RegisteUserOnMalingList = new RegisteUserOnMalingList(repo)
    // const name = 'any_name'
    // const email = 'any@email.com'
    // const response = await useCase.registeUserOnMalingList({ name, email })
    // const user = repo.findUserByEmail('any@email.com')
    // expect((await user).name).toBe('any_name')
  })
})
